<div class="landing-container">
  <%= image_tag ("logo.svg"), class: "icone-landing" %>

  <div class="content-landing">
    <h1>Col.</h1>
    <h1>Une expérience faite pour toi 😉 </h1>

    <h2>La colocation rêver! Simplement au bout de tes doigts !</h2>
  </div>

  <div class="btn-container">

    <% if current_user %>
      <%= link_to "quoi d'neuf ?", colocations_path, class:"btn-landing" %>
    <!------provisoir test---------->
      <%= link_to "Déconnexion ☠️", destroy_user_session_path, method: :delete, class:"btn btn-danger" %>
    <% else %>
      <%= link_to "JE M'INSCRIS", new_user_registration_path, class: "btn-landing" %>
      <%= link_to "CONNEXION", new_user_session_path, class: "btn-secondary" %>
    <% end %>
  </div>
</div>
