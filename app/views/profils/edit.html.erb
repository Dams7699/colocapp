<div id="step_1" class="step step-active text-center">
  <p class="over-titre">étape 1/3</p>
      <%= form_for current_user, url: profil_path do |f| %>
        <h4>Choisis tes traits de personnalité</h4> <br/>
        <div class="content_chekbox">
            <%= f.collection_check_boxes(:personality_ids, Personality.all, :id, :full_name) do |check| %>
              <%= check.check_box class:"checkbox_form" %>
              <%= check.label class:"check_text" do %>
                <%= check.text %>
              <% end %>
            <% end  %>
        </div>
        <br/><%= f.submit "suivant", id:"step_1_next", class:"btn-bottom" %>
      <% end %>
</div>

<div id="step_2" class="step text-center">
  <p class="over-titre">étape 2/3</p>
    <%= form_for current_user, url: profil_path do |f| %>
        <h4>Choisis tes loisirs</h4><br/>
        <div class="content_chekbox">
          <%= f.collection_check_boxes(:hobby_ids, Hobby.all, :id, :full_name) do |check| %>
            <%= check.check_box class:"checkbox_form"%>
            <%= check.label class:"check_text" do %>
              <%= check.text %>
            <% end %>
          <% end  %>
        </div>
      <br/><%= f.submit "suivant", id:"step_2_next", class:"btn-bottom" %>
    <% end %>
</div>

<div id="step_3" class="step text-center">
  <p class="over-titre">étape 3/3</p>

    <%= form_for current_user, url: profil_path do |f| %>
      <h4>Choisis ton ambiance de colocation</h4><br/>
      <div class="content_chekbox">
        <%= f.collection_check_boxes(:goal_ids, Goal.all, :id, :full_name) do |check| %>
          <%= check.check_box class:"checkbox_form"%>
          <%= check.label class:"check_text" do %>
            <%= check.text %>
          <% end %>
        <% end  %>
      </div>

      <%= f.hidden_field :finished, value: "true" %>
      <br/><%= f.submit "suivant", class:"btn-bottom" %>
    <% end %>
</div>
